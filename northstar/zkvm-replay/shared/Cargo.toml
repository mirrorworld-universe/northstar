[package]
name = "northstar-zkvm-replay-shared"
version.workspace = true
edition.workspace = true
publish.workspace = true

[dependencies]
northstar-transaction-proof = { path = "../../transaction-proof", default-features = false, features = ["cycle-tracking"] }
